# host_abstraction (Legacy)

**Status**: Retired — superseded by Gateway + RuntimeDaemon architecture.

This directory contains code from an earlier architectural iteration. It is
preserved for reference only and is not part of the active runtime.

**Active runtime graph**: Console → Gateway → RuntimeDaemon → RuntimeLoop → CapabilityRegistry → Trace → DTO
